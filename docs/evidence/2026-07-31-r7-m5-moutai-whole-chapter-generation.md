# Round 7 M5 — Moutai whole-chapter dossier

贵州茅台 (`600519.SH`) was generated through the same whole-chapter Round 7
runner. Persistent outputs are `artifacts/round7-dossiers/600519.SH.md`,
`.html`, `.receipt.json`, and `.review-queue.json`; replay verification is
`artifacts/evidence/round7-m5-moutai-verification.json`.

- Run: `round7-run:10bc8c41a6fc28e01ce771b4`
- Generated research text: 3,928 characters; chapter body: 6,467; full file: 7,482.
- All eight research chapters are `partial / pending_judgment_review`; the production record is `full`.
- Tier remains B; `target_price`, `position_range`, and `action` remain blocked.
- The verifier records one official source document and an explicit partial-coverage warning. No second document, proxy, or aggregator was invented.

The financial evidence selector excluded one malformed page-level cost row whose
display (`44`) was incoherent with its same-row prior value; the remaining
official fact is retained and the gap is visible in the partial chapter rather
than silently corrected.

The generation reused the CATL-proven Round 7 runner, renderer, validator,
semantic auditor, official narrative capture, and page-fact materializer. It
created no issuer-specific template or field-level fallback.
