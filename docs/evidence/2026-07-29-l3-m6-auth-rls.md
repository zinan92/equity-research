# L3-M6 · Auth, RLS, and secret boundary

Private-preview authentication keeps member/session/entitlement state in a separate external auth database. The release verifier rejects shared research/auth databases; private-preview tests cover entitlement, CSRF, session revocation, and immutable-release protection.

The checked-in Supabase migration enables RLS on canonical schemas and revokes anon/authenticated table privileges. It is a deployment baseline only: no Supabase project is configured, so no live RLS or Storage claim is made. Secret scanning passed; repository runtime state remains excluded.

Validation: `python3 -m unittest product.tests.test_private_preview_v1 product.tests.test_supabase_schema -q` (23 passed, 1 skipped; existing SQLite warnings); `gitleaks detect --no-git --source . --redact` passed.
