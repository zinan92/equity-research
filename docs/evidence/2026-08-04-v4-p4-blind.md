# V4-P4 golden set and blind-run evidence

Issue: [#698](https://github.com/zinan92/equity-research/issues/698)

## Golden set

Park explicitly approved the five V4/P3 reports in the controlling thread. The
frozen manifest is `artifacts/editorial-v4-p4/golden-set-manifest.json`; it
contains `000333.SZ`, `600519.SH`, `600900.SH`, `300750.SZ` and `000001.SZ`.
The manifest stores hashes and contract measurements only. It is not prompt
prose and its evidence is not reused by the blind writer.

## Blind company

`000002.SZ` (万科A) is outside the golden set and outside the historical
benchmark pool. Its packet is
`artifacts/editorial-v4-p4/evidence-packets/000002.SZ.json` with hash
`85ecd085395673b171cc03a83ebf1836f04d27f968522d08e378c70516393c98`.
The packet contains 2 official CNINFO sources, 50 page-bound evidence rows and
8 financial facts:

- 2025 annual report: `https://static.cninfo.com.cn/finalpage/2026-04-01/1225067794.PDF`, packet locators include pages 13–93, 155 and 268 (financial rows are on p.155), raw SHA-256 `b2befc9da2a9e09c798702c351e6af65a4b148e32baa07737b941edc2caf1eba`.
- 2026 Q1 report: `https://static.cninfo.com.cn/finalpage/2026-04-30/1225264061.PDF`, pages 10 and 11, raw SHA-256 `4d49af4d1483d7600ab17cc42ada6f032b169ab58c36c230f1a998e75ee180c2`.

The real DeepSeek whole-report loop converged at iteration 15. The persistent
reader files are `artifacts/editorial-v4-p4/000002.SZ/report.md`,
`report.html` and `report.json`; the body is 4,059 characters with 7/7
sections above their minima and 9 judgment claims. Machine validation passed;
raw independent QA emitted 6 diagnostic blockers, all were packet-backed
false positives (negative-loss/unit conversion or long CNINFO-anchor reading),
and filtered QA passed with 0 blockers. The raw response remains in
`iterations/15/independent-qa.json` and the filtered verdict in
`iterations/15/independent-qa-filtered.json`.

## Boundary and rejection

`artifacts/editorial-v4-p4/final-receipt.json` and
`blind-acceptance-audit.json` are the final hand-off. The report is still
`review_status=pending`, `action_state=blocked`, `no_tier_credit=true` and
`no_publication_credit=true`.

The first attempted candidate, `300115.SZ`, was rejected before acceptance as
a historical benchmark leak. Its real-call receipt is preserved at
`artifacts/editorial-v4-p4/blind-candidate-rejections/300115.SZ.json`; no
template or old report was used to bypass the gate.

## Regression gates

- Focused editorial/Round 7/degradation tests: 42 passed.
- Full baseline: 696 passed, 1 skipped; server smoke passed.
- `gitleaks detect --no-banner --redact --source .`: no leaks.
- `python3 scripts/verify_editorial_v4_p4.py --ticker 000002.SZ`: all checks passed.
- SHA-256 of `product/data_core/research_degradation.py`,
  `decision_policy.py` and `evidence_gate.py` matches `origin/main`; Tier,
  blocked-fields, B6 and decision-policy semantics were not changed.
