# M1–M6 evidence handoff

## Actual result

M1 preserves dual current/prior facts and validates CATL's cross-year revenue
pair. M2 runtime receipt `4502bb32938478ea` has 20 tickers, 52 available
official reports, 68 explicit missing reports and 951 page facts. M3 emitted
20 `no_action` receipts; bank DCF was marked inapplicable. M4 has 7 genuine
page-bound assignments and 13 explicit gaps. This does **not** satisfy #218.

## Five inspectable samples

1. Cross-verified CATL revenue: `1222806982` p119 and `1225002214` p116,
   `362,012,554 千元`.
2. Missing/timeout sample: `600000.SH` records
   `batch_worker_timeout_no_raw_response`, not source unavailability.
3. Share-capital remains separate from shares outstanding; the fact schema
   labels amount versus share-count disclosure and retains its as-of period.
4. CATL DCF uses provisional WACC 9%, terminal growth 3% and five-year horizon.
5. All M3 decision receipts are `no_action`; reasons include
   `insufficient_evidence_coverage`.

## Analyst review priority

1. WACC, terminal growth and forecast period.
2. Bank capital/credit-cost/payout assumptions and model choice.
3. Consumer peer set, premium and consensus source.

## Next step

Repair the PDF worker isolation so it can enforce a per-document wall clock,
then recollect the 13 M4 gaps before inviting any human reviewer.

## Recovery addendum — official PDF transport and native-text checks

PRs #516 and #518 corrected two local failure modes without changing any
evidence, Tier, publication, or decision policy. The outer official-ingestion
runtime now permits the existing bounded transport retry budget to finish, and
the page parser retains short native text instead of replacing it with an empty
OCR result. Manual requests to the CNINFO URLs discovered by the official
structured index yielded valid primary PDFs and recovered page facts for
`600000.SH` and `601857.SH`.

The audit set remains incomplete and #218 remains failed. `600036.SH` has a
valid CNINFO annual-report PDF (`1225047590`); its printed p126 / PDF p127
contains a consolidated balance sheet in RMB millions, but the current
structured page extractor did not yet admit facts from that layout. `000963.SZ`
has official PDF `1225414770`, a foreign-language/scan-style report requiring a
separate page-bounded OCR path. These are parser/OCR gaps, not claims that the
official issuer filings are unavailable. No reviewer should be invited until
each proposed audit row is a real number tied to the same PDF page.
