# L3-M7 · Private delivery state

The private-preview flow verifies invite redemption, server-side entitlement, session revocation, structured feedback audit, and owner suspension. Published release identity remains immutable. Existing tests passed (32; SQLite warnings only).

Editorial publish approval exists in the research path, but the externally reachable private preview deliberately blocks research mutation. A fixed correction SLA and member self-deletion/export are pending; no claim of completion is made for them. UI work remains outside this milestone.
