# Minimal dependencies for the local product dashboard and publication validation.
# The full UZI data pipeline has its own broader requirements at repository root.
Pillow>=10.0,<13
jsonschema>=4.20,<5
