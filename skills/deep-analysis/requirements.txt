# Stock Deep Analyzer · Python dependencies
# 安装: pip install -r requirements.txt
# 然后: playwright install chromium

# A 股 / 港股 / 美股核心数据 (免费、零 key)
akshare>=1.14.0
yfinance>=0.2.40
baostock>=0.8.9    # K 线 fallback (官方接口，无需 key)

# 数据处理
pandas>=2.0.0
requests>=2.31.0

# Web 搜索 (lib/web_search 默认使用，定性维度兜底必装；v2.6.1 论坛 bug 修复)
ddgs>=9.0.0

# K 线渲染 (可选，用于 PNG 出图)
mplfinance>=0.12.10b0

# 报告截图 (Playwright 用真实 Chromium 截图 share-card / war-report)
playwright>=1.42.0

# 颜色和格式
rich>=13.0.0
